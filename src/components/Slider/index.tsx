import React from "react";
import { SliderContiner } from "../SlideContainer";

import { Section } from "./styles";

export const Slider = () => {
  return (
    <Section>
      <SliderContiner />
    </Section>
  );
};
